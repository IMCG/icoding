#include<iostream>
#include<string>
#include<algorithm>
#include<cctype>
using namespace std;
string ConvertToLowerCase(string text)
{
	transform(text.begin(),text.end(),text.begin(),tolower);
	return text;
}
bool IsNotAlpah(char ch)
{
	return !isalpha(ch);
}
bool IsPalindrome(string text)
{
	for(int i=0;i<text.length()/2;i++)
	{
		if(text[i]!=text[text.length()-1-k])
			return false;
	}
	return true;
}
bool IsPalindrome1(string text)
{
	string reversed=text;
	reverse(text.begin(),text.end());
	return reversed==text;
}
bool IsPalindrome2(string text)
{
	return equal(text.begin(),text.begin()+text.size()/2,text.rbegin());
}
bool IsPalindrome3(string text)
{
	text.erase(remove_if(text.begin(),text.end(),IsNotAlpah),text.end());
	transform(text.begin(),text.end(),text.begin(),tolower);
	return equal(text.begin(),text.begin()+text.size()/2,text.rbegin());
}
int main()
{
	string text="ABCDEFG";
}