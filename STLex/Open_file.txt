//STL Algorithms
#include<iostream>
#include<fstream>
#include<tchar.h>
#include<algorithm>
#include<set>
using namespace std;

int main()
{
	fstream file;
	file.open(_TEXT("F:\\Users\\sen\\Desktop\\data.txt"));
	cout<<file.rdbuf();
	file.close();

	ifstream input(_TEXT("F:\\Users\\sen\\Desktop\\data.txt"));//mcro _TEXT
	multiset<int> values;
	//read the data from hte file
	int currValue;
	while(input>>currValue)
		values.insert(currValue);
	//compute the average
	double total=0.0;
	for(auto iter=values.begin();iter!=values.end();iter++)
	{
		total+=*iter;
	}
	cout<<"Average is:"<<total/values.size()<<endl;
}